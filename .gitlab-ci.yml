services:
    - docker:dind

stages:
    - build
    - test
    - deploy

build:
    stage: build
    script: 
        - docker-compose build

test:
    stage: test
    script:
        - docker-compose run frontend npm test -- --watchAll=false
