image: docker/compose:1.29.2

stages:
    - build
    - test
    - deploy

build:
    stage: build
    script: 
        - docker-compose build

test:
    stage: test
    script:
        - docker-compose run frontend npm test -- --watchAll=false
